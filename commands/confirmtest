const Discord = require('discord.js');

exports.run = (client, message, args, func) => {

message.reply("Oof! This command is in progress, so wait until this command is actually made for no--- **How did you find out about this command?!!**")



}
